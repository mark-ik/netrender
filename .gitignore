target/
*~
*#

# WR internals
captures
wrench/json_frames
wrench/ron_frames

# Editors
*.swp
*.swo

# IntelliJ
.idea
*.iws
*.iml

# Gradle
.gradle

# VSCode
.vscode
.vs

# System
.fuse_hidden*
test-logs/
.cargo-check-logs/
netrender/examples/output/

# sem (Ataraxy-Labs) semantic-diff index cache — local, never committed.
.sem/
.build-logs/alpha.err

# Local cargo path overrides (local-dev only). netrender is the leaf of the
# stack and currently needs none, but keep the door ignored so a local override
# (e.g. a local vello/wgpu checkout) can never be committed by accident.
.cargo/
